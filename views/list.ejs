<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>To Do List</title>
</head>
<body>
    <h1>It's a <%= typeOfDay %></h1>

    <ul>
        <% for( let index = 0; index <newListItems.length; index++ ) { %>
            <li><%= newListItems[index] %></li>
        <% } %>
    </ul>

    <form method="post" action="/">
        <input type="text" name="newItem" placeholder="New Task">
        <button type="submit">Add task</button>
    </form>
</body>
</html>